version: '3.8'
services:
  # postgres:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_USER: dev
  #     POSTGRES_PASSWORD: dev
  #     POSTGRES_DB: demo
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  payments:
    build: ./backend/payments
    env_file: ./backend/payments/.env
    depends_on:
      # - postgres
      - redis
    ports:
      - "8080:8080"
      # - "5678:5678"  # Port for debugger
    # volumes:
    # - ./backend/payments/app:/app   # Mount local code inside container so that reload can work

volumes:
  pgdata:
